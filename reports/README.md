# Reports

This directory collects repository-level outputs that summarize experiments,
portfolio material, and visual artifacts.

## Layout

- `figures/` - reusable or aggregated figures.
- `tables/` - reusable or aggregated tabular outputs.
- `portfolio/` - polished material for showcase and review.

Per-project reports stored under project-local directories remain valid.
