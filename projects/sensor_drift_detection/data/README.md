# Data

This project generates synthetic telemetry during execution and writes any
derived artifacts under local processed outputs as needed.

The directory exists to document the data contract and keep the project aligned
with the repository-wide project template.
