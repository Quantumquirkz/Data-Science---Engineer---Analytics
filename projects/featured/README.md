# Featured Projects

Featured projects are the strongest portfolio candidates. Their canonical paths
remain at `projects/<slug>/` during the first migration wave.

Initial featured set:

- `sensor_drift_detection`
- `gravitational_orbit_simulator`
- `renewable_energy_mix_optimizer`
- `particle_diffusion_mc`
