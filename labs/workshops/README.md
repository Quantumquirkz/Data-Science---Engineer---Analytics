# Workshops

This directory contains normalized workshop paths grouped by topic.

The historical `Workshops/` directory remains as a compatibility surface with
symlinks back to these canonical locations.
