# Model Artifacts

This directory is reserved for repository-level model bundles and serialized
artifacts that should not live inside source directories.

## Policy

- Small demonstrative artifacts may be committed when clearly justified.
- Large binary artifacts should stay out of Git by default.
- Per-project local model outputs remain valid during migration.
- The directory is intentionally DVC-ready for later adoption.
